# Never add model weights, environments, or generated compiler caches.
*.safetensors
*.gguf
.venv/
__pycache__/
.cache/
core
core.*


# Green-context experiment: build artifacts + large nsys/ncu reports
agent_space/experiments/2026-07-27-green-context-marlin/green_context_probe
agent_space/experiments/2026-07-27-green-context-marlin/nsys/*.nsys-rep
agent_space/experiments/2026-07-27-green-context-marlin/ncu/*.ncu-rep

# Shared-memory monopoly experiment: compiled probes (rebuild with nvcc).
# Paths are relative to this file; the repo root is agent_space itself, so an
# "agent_space/" prefix never matches - the green-context entries above have the
# same defect and are left alone rather than silently changing their behaviour.
experiments/2026-07-29-marlin-smem-monopoly/analysis/smem_monopoly
experiments/2026-07-29-marlin-smem-monopoly/analysis/tier_union
